version: '3.8'

services:
  backend:
    build: .
    container_name: network-backend
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - network-app
    restart: unless-stopped

networks:
  network-app:
    driver: bridge
